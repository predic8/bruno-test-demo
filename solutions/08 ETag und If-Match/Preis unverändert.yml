info:
  name: "Preis unverändert"
  type: http
  seq: 5

http:
  method: GET
  url: "{{baseUrl}}/products/{{productId}}"
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("Abgelehntes Update hat nichts verändert", function () {
          expect(res.getBody().price).to.equal(1.5);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
