info:
  name: "Rambutan Details"
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/products/12"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false

docs: |-
  Die id 12 stammt aus der Suche. Preis steht im Feld `price`.
