info:
  name: "Rambutan suchen"
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/products?name=Rambutan"
  params:
    - name: name
      value: "Rambutan"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
