info:
  name: "Rambutan Bild"
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseUrl}}/products/12/image"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
